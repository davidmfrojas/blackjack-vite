import { defineConfig } from "vite";

export default defineConfig({
    base: '/blackjack-vite/', //base para el path del proyecto y cargarlo en el sitio, es el nombre del repositorio de github donde se ha publicado
});